<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Libmemo.TreePage"
             xmlns:local="clr-namespace:Libmemo"
             NavigationPage.HasNavigationBar="False"
             >
    <ContentPage.Content>
        <StackLayout HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
            <StackLayout Orientation="Horizontal" Padding="10" HorizontalOptions="FillAndExpand">
                <Button Text="Отправить"
                    Clicked="Search_Button_Clicked"
                    HorizontalOptions="FillAndExpand"
                    />
                <Button Text="Сбросить"
                    Clicked="Reset_Button_Clicked"
                    HorizontalOptions="FillAndExpand"
                    />
            </StackLayout>
            <StackLayout>
                <Slider Minimum="0" Maximum="100" Value="100" ValueChanged="Slider1_ValueChanged"/>
            </StackLayout>
            <local:CustomScrollView x:Name="scroll" Orientation="Both" MaximumZoom="1" MinimumZoom="0.05">
                <AbsoluteLayout x:Name="absolute" />
            </local:CustomScrollView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>